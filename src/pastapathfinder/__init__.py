"""pastapathfinder — sliced call traces for legacy and AI-generated Python codebases.

Two programs sharing one artifact (design.md §1): the batch analysis pipeline
(`pastapathfinder analyze`) and the local read-only viewer (`pastapathfinder view`).
"""

__version__ = "0.1.0"
