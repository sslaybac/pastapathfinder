"""AST walk: nodes, spans, and the call-resolution ladder (design.md §3.5, D16).

FR-12, FR-14, FR-37, FR-40.
Placeholder: nodes and `contains`/`imports` edges land in specs/tasks.md task 2.2;
the `calls` ladder and ambiguity flags in task 2.3.
"""
